user:
  name: "Bob"
  github: "bobdev"

runtime:
  image: "python:3.11-slim"

commands:
  package: "inxi"
  build:
    - "pip install -r requirements.txt"
  start: "python api.py"

repository:
  branch: "main"
  auto_deploy: true
